---
import Layout from '../layouts/Layout.astro';
import Card from '../components/Card.astro';
import type { SignatureKind } from 'typescript';
import WindowCard from '../components/WindowCard.astro';
import Snowfall from 'react-snowfall'


---

<Layout title="Adventskalender 2024">
	<main>
		<Snowfall
  color="white"
  snowflakeCount={200}
  client:load
  style={{
    position: 'fixed',
    width: '100vw',
    height: '100%',
  }}
/>
		
		<h1> <span class="text-gradient">🎄🎅 Adventskalender 🎅🎄</span></h1>
		<p>Ho ho ho! Hvilke varianter har vært snille i år?</p>
		<ul role="list" class="link-card-grid">
			{[...Array(24).keys()].map((_, index) => (
				<WindowCard
				  href={"/"+String(index + 1)}
				  title={String(index + 1)}
				  body="Åpnes snart..."
				/>
			  ))}
		</ul>
		
	</main>
</Layout>

<style>
	main {
    margin: auto;
    padding: 1rem;
    width: 1000px;
    max-width: calc(100% - 2rem);
    color: white;
    font-size: 20px;
    line-height: 1.6;
    background-color: #004400; /* Dark green background */
    border-radius: 10px;
    box-shadow: 0px 0px 15px 2px rgba(0, 0, 0, 0.2); /* Festive glow */
}

.astro-a {
    position: absolute;
    top: -32px;
    left: 50%;
    transform: translatex(-50%);
    width: 220px;
    height: auto;
    z-index: -1;
    background-image: url('https://cdn-icons-png.flaticon.com/512/2530/2530064.png'); /* Add a snowflake or star icon */
    background-size: cover;
}

h1 {
    font-size: 4rem;
    font-weight: 700;
    line-height: 1;
    text-align: center;
    margin-bottom: 1em;
    color: #FF0000; /* Christmas red for the title */
}

.text-gradient {
    background-image: linear-gradient(90deg, #FFDD00, #FF0000, #008000); /* Christmas colors in gradient: gold, red, green */
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-size: 400%;
    background-position: 0%;
    animation: gradientShift 3s infinite ease-in-out;
}

@keyframes gradientShift {
    0% { background-position: 0%; }
    100% { background-position: 100%; }
}

.instructions {
    margin-bottom: 2rem;
    border: 1px solid rgba(255, 255, 255, 0.25); /* Light frosty border */
    background: linear-gradient(rgba(0, 128, 0, 0.66), rgba(0, 128, 0, 0.33)); /* Green gradient background */
    padding: 1.5rem;
    border-radius: 8px;
    box-shadow: 0px 0px 15px 2px rgba(255, 0, 0, 0.2); /* Subtle red glow */
}

.instructions code {
    font-size: 0.8em;
    font-weight: bold;
    background: rgba(255, 255, 255, 0.12);
    color: #FFD700; /* Gold code text */
    border-radius: 4px;
    padding: 0.3em 0.4em;
}

.instructions strong {
    color: #FFD700; /* Gold strong text */
}

.link-card-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 2rem;
    padding: 0;
    background-color: #006400; /* Christmas green background for the grid */
    border-radius: 8px;
    padding: 1.5rem;
}
</style>
